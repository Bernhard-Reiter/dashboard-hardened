# Sentry Configuration
# https://sentry.io

# Build-time (required for source map upload)
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=

# Runtime (required for error tracking)
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=

# Optional: Control sampling rate (default: 0.1 prod, 1.0 dev)
SENTRY_TRACES_SAMPLE_RATE=0.1
NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE=0.1

# Optional: Enable Sentry test page at /sentry-test (DO NOT enable in production)
# NEXT_PUBLIC_ENABLE_SENTRY_TEST=true

# Optional: DSN allowlist for tunnel endpoint (comma-separated public keys)
# Extract public key from NEXT_PUBLIC_SENTRY_DSN: https://<publicKey>@...
# SENTRY_ALLOWED_PUBLIC_KEYS=abc123def456,xyz789ghi012

# OpenTelemetry (Future Phase 2 - vendor-agnostic tracing)
# Uncomment and configure when adding OTel exporter
# OTEL_SERVICE_NAME=dashboard-hardened
# OTEL_EXPORTER_OTLP_ENDPOINT=https://your-otlp-endpoint
# OTEL_EXPORTER_OTLP_HEADERS=Authorization=Bearer <token>

# Synthetic Health Check (GitHub Secrets)
# HEALTHCHECK_URL=https://your-app.vercel.app/api/health
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
